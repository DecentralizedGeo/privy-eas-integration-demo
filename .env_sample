# Client-side environment variables (exposed to Vite/browser)
# Copy to .env.vite or .env.local and fill in with your values
VITE_PRIVY_APP_ID=<your_privy_app_id>

# Server-side only environment variables (NOT exposed to Vite client)
# These should NEVER have VITE_ prefix
# Add these to .env only if you have a backend server
PRIVY_APP_ID=<your_privy_app_id>